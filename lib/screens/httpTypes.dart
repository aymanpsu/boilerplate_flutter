import 'package:flutter/material.dart';

class HttpTypes extends StatelessWidget {
  const HttpTypes({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("data"),
    );
  }
}